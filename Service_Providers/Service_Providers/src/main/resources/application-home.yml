spring:
  datasource:
    url: jdbc:mysql://localhost:3306/service_providers?createDatabaseIfNotExist=true
    username: root
    password: PkNLm@&$2357
  jpa:
    properties:
      hibernate:
        '[format_sql]': true
    hibernate:
      ddl-auto: create-drop
    show-sql: true
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
logging:
  level:
    web: debug